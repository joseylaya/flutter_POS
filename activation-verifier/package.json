{
  "name": "jmpos-activation-verifier",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=20"
  }
}
